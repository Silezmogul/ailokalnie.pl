# Ignore everything by default
*

# Allow website (only tracked content)
!index.html
!static/
!static/**
!hero.png
!CNAME
!.github/
!.github/**

# Keep .gitignore itself tracked
!.gitignore

# Do not ignore directories so git can traverse for un-ignores
!*/
